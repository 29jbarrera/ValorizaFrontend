import { FiltrogruasPipe } from './filtrogruas.pipe';

describe('FiltrogruasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrogruasPipe();
    expect(pipe).toBeTruthy();
  });
});
